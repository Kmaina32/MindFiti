"use client";

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function AssessmentPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>AI Wellness Assessments</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is where the AI Wellness Assessments will be.</p>
      </CardContent>
    </Card>
  );
}
